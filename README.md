# cte-commander

## Usage

0. Install docker on your local machine.
1. Run `yarn`
2. Set the environment variables while referencing to `.env.example`.
3. Run `yarn start ${FileNameYouWantToRun}`
   e.g.) `yarn start src/warmup/callMeIsComplete.ts`
